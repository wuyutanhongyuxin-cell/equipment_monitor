#pragma once

// Copy this file to secrets.h and enter the local 2.4 GHz WiFi credentials.
// secrets.h is excluded by .gitignore and must never be committed.
constexpr char WIFI_SSID[] = "replace-with-wifi-name";
constexpr char WIFI_PASSWORD[] = "replace-with-wifi-password";
constexpr char WIFI_FACTORY_SSID[] = "replace-with-second-wifi-name";
constexpr char WIFI_FACTORY_PASSWORD[] = "replace-with-second-wifi-password";
